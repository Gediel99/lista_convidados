# Lista do casamento

App Streamlit simples para editar convidados, marcar favoritos com estrela, remover com `X` e separar por familia.

## Rodar

```powershell
pip install -r requirements.txt
streamlit run app.py
```

Os dados ficam salvos em `dados_casamento.json` na mesma pasta do app.
